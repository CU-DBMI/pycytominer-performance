# requirements from ansible-galaxy can be specified here; they'll be installed
# by ansible_on_tfinv.sh if INSTALL_REQS=1. otherwise, you can install them
# manually via ansible-galaxy install -r requirements.yml

# (an example of installing a letsencrypt role is included below)

# ---
# roles:
# - name: systemli.letsencrypt
#   version: 2.1.0
